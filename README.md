# IT Infrastructure & DevOps Trainee Assignment
created ec2 instance and connected to it via wsl
created trainee with sudo privileges
configured ssh key, allowed ssh via 2222 port disabled direct ssh login
configured ufw for port 2222,80,443
installed docker and tested through hello world image
ran reverse proxy in nginx, backend on port 5000, postgres with persistent storage via docker container
bashscripting and automation for usage and running with cron
added backupscript with compression, restoration command to recover from backup archive
added metrics collection with nginx endpoint
