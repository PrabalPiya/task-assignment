server {
    listen 8080;
    server_name _;

    location = /metrics {
        alias /usr/share/nginx/metrics/metrics.txt;
        default_type text/plain;
        add_header Cache-Control "no-store";
    }

    location / {
        return 404;
    }
}
